baseURL = "https://ihanachurch.org/"
title = "아이하나 교회"
defaultContentLanguage = "ko"
locale = "ko"

[pagination]
pagerSize = 3


# Exclude footer section from generating list pages (index.html and index.xml)
[outputs]
  page = ["HTML"]
  home = ["HTML", "RSS", "JSON"]
  section = ["HTML", "RSS"]
  taxonomy = ["HTML", "RSS"]
  term = ["HTML", "RSS"]
  footer = []

[taxonomies]
  tag = "tags"
  category = "categories"
  author = "authors"
  series = "series"
  topic = "topics"

# Explicitly prevent the footer section from generating any output
[outputFormats.footerSection]
  # Empty configuration to prevent any output

# Specifically disable generation of these kinds for the footer content
disableKinds = ["footerSection"]

theme = "adritian"

[module]
  ## Base mounts - so your site's assets are available
  [[module.mounts]]
    source = "themes/adritian/archetypes"
    target = "archetypes"
  [[module.mounts]]
    source = "archetypes"
    target = "archetypes"

  [[module.mounts]]
    source = "themes/adritian/assets"
    target = "assets"
  [[module.mounts]]
    source = "assets"
    target = "assets"

  [[module.mounts]]
    source = "themes/adritian/i18n"
    target = "i18n"
  [[module.mounts]]
    source = "i18n"
    target = "i18n"

  [[module.mounts]]
    source = "themes/adritian/layouts"
    target = "layouts"
  [[module.mounts]]
    source = "layouts"
    target = "layouts"

  [[module.mounts]]
    source = "themes/adritian/static"
    target = "static"
  [[module.mounts]]
    source = "static"
    target = "static"

  # The following mounts are required for the theme to be able to load bootstrap
  # Remember also to copy the theme's `package.json` to your site, and run `npm install`
  [[module.mounts]]
    source = "node_modules/bootstrap/scss"
    target = "assets/scss/bootstrap"

  [[module.mounts]]
    source = "node_modules/bootstrap/dist/js"
    target = "assets/js/bootstrap"

  [[module.mounts]]
    source = "node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"
    target = "assets/js/vendor/bootstrap.bundle.min.js"

  [[module.mounts]]
    source = "node_modules/bootstrap-print-css/css/bootstrap-print.css"
    target = "assets/css/bootstrap-print.css"


[params]
title = '아이하나 교회 | 뉴저지 포트리 · 조용한 위로와 회복이 있는 곳'
description = '뉴저지 포트리(Fort Lee)에 위치한 해외한인장로회(KPCA) 소속 아이하나 교회입니다. 말씀을 기억하며 슬픔과 상처를 치유하는 예배의 자리로 당신을 초대합니다.'
images = ['/img/og-img.png']
themeColor = '#E8F0F8'
logo = '/img/logo.svg'

# Color scheme: default | ocean | forest | rose | slate | midnight | warm
colorScheme = 'default'

# Enable the live color scheme switcher (for the demo site)
[params.colorSchemeSwitcher]
enable = false

# Color scheme selector visibility — set to true to hide in that location
[params.colorSchemeSwitcher.selector.disable]
footer = true
header = true

[params.seo]
siteName = '아이하나 교회'
siteDescription = '뉴저지 포트리(Fort Lee)에 위치한 해외한인장로회(KPCA) 소속 아이하나 교회입니다. 말씀을 기억하며 슬픔과 상처를 치유하는 예배의 자리로 당신을 초대합니다.'
siteImage = '/img/og-img.png'

[params.social]
twitter = ''

[params.seo.person]
name = '최세나 목사'
url = 'https://ihanachurch.org'
sameAs = []
sections = [
  "showcase",
  "about",
  "worship",
  "location"
]
# If you want to display an image logo in the header, you can add it here
# logo = '/img/hugo.svg'

homepageExperienceCount = 6

[params.favicons]
# Optional: provide a different source image for auto-generation
source = "icons/favicon.png"

# Optional: override any generated file with a static one
# favicon = "/favicon.ico"
# icon16 = "/icons/icon-16.png"
# icon32 = "/icons/icon-32.png"
# appleTouch = "/icons/apple-touch-icon.png"
# icon192 = "/icons/icon-192.png"
# icon512 = "/icons/icon-512.png"
# manifest = "/site.webmanifest"

[params.analytics]
## Analytics parameters.
### Supported so far: Vercel (Page Insights, Analytics)
### And Google (Tag Manager, Analytics)

# controls vercel page insights - disabled by default
# to enable, just set to true
vercelPageInsights = false
vercelAnalytics = false

# google analytics and tag manager. to enable, set "enabled" to true
# and add the tracking code (UA-something for analytics, GTM-something for tag manager)
[params.analytics.googleAnalytics]
code = "UA-XXXXX-Y"
enabled = false
[params.analytics.googleTagManager]
code = "GTM-XXXXX"
enabled = false

[build]
writeStats = true

[build.buildStats]
enabledFor = ["scss"]
disableClasses = false
disableIDs = false
disableTags = false
enable = true

# Language selector visibility — set to true to hide in that location
[params.languages.selector.disable]
footer = false
header = false
mobileHeader = false

[languages]
[languages.ko]
disabled = false
locale = 'ko'
direction = 'ltr'
label = '한국어'
title = '아이하나 교회'
weight = 1

[languages.ko.menus]
[[languages.ko.menus.header]]
name = "홈"
URL = "/"
weight = 1
[[languages.ko.menus.header]]
name = '교회 소개'
URL = '/about/'
weight = 2
[[languages.ko.menus.header]]
name = '예배 안내'
URL = '/worship/'
weight = 3
[[languages.ko.menus.header]]
name = '설교/말씀'
URL = '/sermons/'
weight = 4
[[languages.ko.menus.header]]
name = '오시는 길'
URL = '/location/'
weight = 5
[[languages.ko.menus.header]]
name = '헌금 안내'
URL = '/giving/'
weight = 6
[[languages.ko.menus.header]]
name = '기도 요청'
URL = '/prayer/'
weight = 7
[[languages.ko.menus.header]]
name = '문의하기'
URL = '/contact/'
weight = 8

[[languages.ko.menus.footer]]
name = "홈"
URL = "/"
weight = 1
[[languages.ko.menus.footer]]
name = "교회 소개"
URL = "/about/"
weight = 2
[[languages.ko.menus.footer]]
name = "예배 안내"
URL = "/worship/"
weight = 3
[[languages.ko.menus.footer]]
name = "오시는 길"
URL = "/location/"
weight = 4

[languages.en]
disabled = true

[languages.es]
disabled = true

[languages.fr]
disabled = true

[languages.ar]
disabled = true

[languages.he]
disabled = true


# Plugins
[params.plugins]

# CSS Plugins
[[params.plugins.css]]
URL = "css/custom.css"
[[params.plugins.css]]
URL = "css/adritian-icons.css"
## Print css
[[params.plugins.css]]
URL = "css/bootstrap-print.css"

# JS Plugins
[[params.plugins.js]]
URL = "js/rad-animations.js"
[[params.plugins.js]]
URL = "js/sticky-header.js"
[[params.plugins.js]]
URL = "js/library/fontfaceobserver.js"

# SCSS Plugins
[[params.plugins.scss]]
URL = "scss/adritian.scss"


# theme/color style 
[params.colorTheme]

## the following configuration would disable automatic theme selection
#  [params.colorTheme.auto]
#    disable = true
#  [params.colorTheme.forced]
#    theme = "dark"

## the following parameter will disable theme override in the footer
#  [params.colorTheme.selector.disable]
#  footer = true

# Color theme selector visibility — set to true to hide in that location
[params.colorTheme.selector.disable]
footer = false
header = false
mobileHeader = false
## by default we allow override AND automatic selection

# View Transitions API - enabled by default
# Set to false to disable the native View Transitions API
# viewTransitions = false

[params.sharing]
bluesky = true
mastodon = true

[params.blog]
layout = "sidebar-right"     # options: default (no sidebar), sidebar, sidebar-right
sidebarWidth = "25"    # percentage width of the sidebar
narrowContent = true   # if true, limits content width to 720px for better readability
showCategories = true
showRecentPosts = true
recentPostCount = 5
listStyle = "summary"  # options: simple, summary
listLayout = "default" # options: default, cards
featured_sort_by_weight = true
featured_sort_by_date = false


[params.experience]
showJobCard = true # Set to false to hide the job card

# Navbar overflow handling - options: "dropdown" | "footer" | "none"
[params.navbarOverflow]
mode = "dropdown" # "dropdown" moves items to More dropdown, "footer" moves to footer, "none" disables


# Performance optimizations
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.highlight]
    lineNos = false
    noClasses = false
    style = "github"

[imaging]
  resampleFilter = "Lanczos"
  quality = 85
  anchor = "Smart"

[minify]
  disableCSS = false
  disableHTML = false
  disableJS = false
  disableJSON = false
  disableSVG = false
  disableXML = false
  minifyOutput = true
  [minify.tdewolff]
    [minify.tdewolff.html]
      keepDefaultAttrVals = true
      keepWhitespace = false
    [minify.tdewolff.css]
      precision = 0
    [minify.tdewolff.js]
      precision = 0
